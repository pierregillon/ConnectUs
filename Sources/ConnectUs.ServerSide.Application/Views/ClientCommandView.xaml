<UserControl x:Class="ConnectUs.ServerSide.Application.Views.ClientCommandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:ConnectUs.ServerSide.Application.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{Binding Source={StaticResource DesignTimeLocator}, Path=ClientCommandViewModel}">
    <Grid>
        <controls:CommandLineTextBox CommandLines="{Binding Path=CommandLines, Mode=TwoWay}"
                                     VerticalScrollBarVisibility="Auto"
                                     FontFamily="Consola"
                                     FontSize="14"
                                     Background="Black"
                                     Foreground="LawnGreen"
                                     FontWeight="SemiBold"/>
    </Grid>
</UserControl>