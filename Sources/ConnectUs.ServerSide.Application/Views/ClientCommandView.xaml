<UserControl x:Class="ConnectUs.ServerSide.Application.Views.ClientCommandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding Source={StaticResource Locator}, Path=ClientCommandViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" AcceptsReturn="True" IsReadOnly="True" Text="{Binding Path=History, Mode=OneWay}"/>
        <TextBox Grid.Row="1" Text="{Binding Path=CurrentCommand, Mode=TwoWay}" />
    </Grid>
</UserControl>